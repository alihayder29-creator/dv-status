# Demand Vintage Status

Remote status file for the Demand Vintage extension.

Use `status.json` to pause or resume the extension.

Enabled:

```json
{
  "enabled": true,
  "message": ""
}
```

Disabled:

```json
{
  "enabled": false,
  "message": "Demand Vintage is temporarily paused."
}
```
